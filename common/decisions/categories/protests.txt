MD_protests_forming = {
	allowed = { NOT = { original_tag = ZOM } }
	icon = GFX_decision_category_generic
	priority = 8000

	visible = {
		NOT = { has_country_flag = disabled_protests_system }
		OR = {
			check_variable = { protest_strength > 20 }
			has_country_flag = MD_protest_mission_active
			has_country_flag = MD_protest_cooldown
		}
	}
}
